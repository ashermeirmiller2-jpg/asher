# Frontend: leave blank to use the same Vercel deployment's /api function.
# Set this only when the API is deployed on a different origin.
REACT_APP_BACKEND_URL=

# Backend / Vercel Function settings.
MONGO_URL=mongodb+srv://user:password@cluster.example.mongodb.net/?retryWrites=true&w=majority
DB_NAME=munchys_grill
CORS_ORIGINS=*
